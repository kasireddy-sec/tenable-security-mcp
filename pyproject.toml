[project]
name = "tenable-security-mcp"
version = "0.1.0"
description = "Unified MCP server for Nessus and Tenable Vulnerability Management"
requires-python = ">=3.11"
dependencies = [
    "mcp>=2.0.0",
    "httpx>=0.27.0"
]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
